{
  "name": "@arm64js/protocol",
  "version": "1.0.0",
  "description": "The protocol between the arm64js SDK and the arm64js engine runtime: API types, frame handshake, RPC messages and client, error codes.",
  "type": "module",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/kooler/arm64js-sdk.git",
    "directory": "protocol"
  },
  "homepage": "https://arm64js.com",
  "sideEffects": false,
  "exports": {
    ".": {
      "types": "./dist/protocol.d.ts",
      "default": "./dist/protocol.js"
    },
    "./rpc-client": {
      "types": "./dist/rpc-client.d.ts",
      "default": "./dist/rpc-client.js"
    }
  },
  "files": [
    "dist",
    "README.md"
  ],
  "scripts": {
    "build": "node -e \"require('node:fs').rmSync('dist', { recursive: true, force: true })\" && tsc -p tsconfig.json",
    "prepack": "npm run build"
  }
}
